# data.py
import numpy as np
import h5py

def load_data(file_path):
    # Load your data from file (e.g., CSV, images, etc.)
    # Preprocess and return data and labels
    return X_train, y_train, X_val, y_val

